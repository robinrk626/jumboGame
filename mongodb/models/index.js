const UserModel = require('./userModel');
const QuestionsModel = require('./questionModel');
const UserSessionModel = require('./userSessionModel');
const GameModel = require('./gameModel');

module.exports = {
  UserModel,
  QuestionsModel,
  UserSessionModel,
  GameModel,
};